---
plugin: Transitionist
stage: 3
phase: 5.1
status: complete
last_updated: 2026-08-26
complexity_score: 5.0
phased_implementation: true
orchestration_mode: true
next_action: continue_gui_agent_phase_5_2
next_phase: 5.2
ready_for_implementation: true
contract_checksums:
  creative_brief: sha256:c1ae2a332ccc174c8db3262ab4ae06f5a5c59d9adda9531a6a7bcab258658dd8
  parameter_spec: sha256:e383cfd58b8aa63d7d52dd60ec91d61fe3acffbce0ae3dad073b4ce8711667bc
  architecture: sha256:0610ea64183a6da9d9e5fadba2ef036f8e5c39333a83fa07240fd5a70958c10f
  plan: sha256:e9901568f5c6d3946f5bb5ddcb2b173727767af3c2cf5e3d16cc79f658385913
---

# Resume Point

## Current State: Stage 3 GUI - Phase 5.1 (Layout and Basic Controls) Complete

Integrated the pre-generated v4 WebView scaffolding into the real plugin
project structure (INTEGRATION only, not new design work - per plan.md
Phase 5.1 scope). `Source/ui/public/index.html` copied from the finalized
production `v4-ui.html`; JUCE's WebView JS bridge (`js/juce/index.js`,
`js/juce/check_native_interop.js`) copied verbatim from GainKnob (identical
across this codebase's WebView plugins, per v4-integration-checklist.md's
explicit instruction). `Source/PluginEditor.h/.cpp` replaced entirely with
the real WebView editor, adapted from `v4-PluginEditor.h/.cpp` templates:
the only adaptation needed was `processorRef.apvts` -> `processorRef.parameters`
(this codebase's actual APVTS member name, per Stage 1's established
convention) - class name `TransitionistAudioProcessor` in the template
already matched exactly, no other changes needed. 3 relays
(`WebSliderRelay`: throwRelay/spaceRelay/sweepRelay) -> WebView
(`.withNativeIntegrationEnabled()`, explicit `getResource()` URL mapping
per Pattern #8, `.withOptionsFrom()` for all 3 relays) -> 3 attachments
(`WebSliderParameterAttachment`, 3-arg constructor with `nullptr`
undoManager per Pattern #12), declared/constructed in that exact order per
Pattern #11. `setSize(980, 420)` + `setResizable(false, false)` per
v4-ui.yaml. `sweep` bound identically to throw/space (no bipolar-specific
C++ code needed, per parameter-spec.md's explicit note - bipolar behavior
lives entirely in APVTS range (already done in Stage 1) and in
`index.html`'s `makeBipolarRenderer()` (already implemented in the
production HTML)). CMakeLists.txt's previously-commented WebView resource
block uncommented as-is (`juce_add_binary_data(Transitionist_UIResources
...)`, linked privately to the main target, `JUCE_WEB_BROWSER=1`/
`JUCE_USE_CURL=0` defined) - `juce::juce_gui_extra` and `NEEDS_WEB_BROWSER
TRUE` were already present from Stage 1, untouched. Per gui-agent's role
boundaries, no build/cmake commands were run - build-automation skill
verifies compilation and DAW load next. Phase 5.2 (parameter
binding/interaction verification, incl. sweep's bipolar behavior) and 5.3
(visual polish regression) remain, per plan.md's phase split - not in scope
for this phase.

## Previous State: Stage 2 DSP - Phase 4.3 (Advanced Features) Complete - STAGE 2 DSP FULLY DONE

Phase 4.3 (the final DSP phase) implemented in `Source/PluginProcessor.h/.cpp`,
extending Phase 4.2's chain with the last three architecture.md components,
inserted at their exact specified chain positions: (1) Reverb Modulation
(Component 5) - a hand-rolled sine-wave phase accumulator (~0.15Hz) driving a
new, independently-sized `juce::dsp::DelayLine` (Lagrange3rd, max 15ms,
separate from the main tempo-synced delay's buffer) inserted immediately after
`reverb.process()` and before the Bipolar DJ Filter; modulates delay time
2-8ms (5ms center, +/-3ms depth via sine LFO), applied unconditionally
(including during freeze) per architecture.md's "keep the frozen tail alive"
rationale - primary hand-rolled approach used successfully, Chorus fallback
NOT needed; (2) Output Glue (Component 7) - `juce::dsp::WaveShaper` with
`tanh(1.15f * x)` soft-clip followed by `juce::dsp::Limiter` (-1.0dB
threshold, 50ms release), inserted after the Bipolar DJ Filter's crossfade
recombination and before Stereo Width, wet-only; (3) Stereo Width
(Component 8) - manual M/S encode/decode with fixed 1.35x side gain,
inserted after Output Glue and before the Dry/Wet Mixer, guarded for
numChannels >= 2 (defensive no-op on unexpected mono buffers), wet-only.
Final chain order verified against architecture.md's Processing Chain
diagram: Delay -> Reverb -> Reverb Modulation -> Bipolar Filter -> Output
Glue -> Width -> Dry/Wet Mix. All 9 architecture.md DSP components are now
implemented; Stage 2 DSP is complete. Build/DAW/pluginval verification NOT
run by dsp-agent per its role boundaries - handed off to plugin-workflow's
build-automation skill.

## Previous State: Stage 2 DSP - Phase 4.2 (Parameter Modulation) Complete

Phase 4.2 implemented in `Source/PluginProcessor.h/.cpp`, extending Phase
4.1's 6-stage serial chain with two additions per architecture.md/plan.md:
(1) reverb freeze/hold - `freeze = smoothstep(throwNorm, 0.90, 1.00)` drives
`reverbParams.freezeMode` (was hardcoded 0.0), plus an explicit
`reverbInputGain = 1.0 - freeze` applied via `buffer.applyGain()` immediately
before `reverb.process()` so the already-frozen tail keeps sustaining while
only the NEW input is muted; (2) throw-scaled feedback saturation drive -
`drive = 1.0 + 0.6 * throwNorm` (was hardcoded 1.0) in the hand-built delay
feedback loop. Both additions reuse the existing `smoothstep()` helper and
`throwNorm` local (no duplication, no new member variables - both are local
per-block computations). NOT yet implemented (deferred to Phase 4.3):
reverb modulation (LFO micro-delay), Output Glue (soft-clip/limiter), Stereo
Width (M/S). Build/DAW verification NOT run by dsp-agent per its role
boundaries - handed off to plugin-workflow's build-automation skill.

## Previous State: Stage 2 DSP - Phase 4.1 (Core Processing) Complete

Phase 4.1 implemented full 6-stage serial chain (dry tap -> tempo-synced
delay w/ hand-built tanh+TPT feedback loop -> basic `juce::dsp::Reverb` (no
freeze) -> dual always-running `LadderFilter` bipolar crossfade -> equal-power
dry/wet mix).

## Previous State: Stage 1 - Foundation + Shell Complete

Foundation + Shell complete: CMakeLists.txt (VST3/AU/Standalone, stereo-in/
stereo-out effect bus config, no MIDI/IS_SYNTH) and APVTS with all 3 locked
parameters (`throw`, `space`, `sweep`) implemented exactly per
parameter-spec.md, including `sweep`'s bipolar range/midpoint verification.
`processBlock()` is an intentional pass-through stub (no DSP yet - Stage 3
DSP owns all 9 architecture.md components). PluginEditor is a minimal
placeholder (Stage 3 GUI replaces it with the already-scaffolded v4 WebView
editor). Ready to proceed to Stage 3 DSP (Phase 4.1).

## Completed So Far

**Post-verification UX refinement (2026-08-26, live-tested via Standalone app):** Complete
- User requested a center-detent "snap" on SWEEP while dragging: value now
  briefly snaps to and holds at exactly 0% for ~150ms when a drag passes
  through a small deadzone around center (+/-1.5% normalised), then resumes
  tracking smoothly from the pointer's current position - mimics a physical
  pot's center detent
- Implemented via a new `centerDetent` flag on the shared `setupKnob()`
  interaction wiring (only passed `true` for the `sweep` knob's setup call);
  pointer-drag only - wheel/keyboard nudging and host automation/preset
  recall are unaffected (both must still pass through 0.5 normalised
  continuously and immediately)
- Applied identically to all 3 HTML copies that need to stay in sync:
  `Source/ui/public/index.html` (shipped/embedded), `.ideas/mockups/v4-ui.html`
  (production template source of truth), and `.ideas/mockups/v4-ui-test.html`
  (browser-testable mockup) - each adapted to that file's own drag pattern
  (production uses the relative per-frame-delta pattern; the test mockup
  uses the absolute-anchor pattern)
- Rebuilt + reinstalled (VST3 4.3M / AU 4.2M, fresh binary confirmed via
  `centerDetent` string present in the compiled BinaryData and matching
  build timestamp) and relaunched in the Standalone app for live testing
- `v4-ui.yaml` updated with a new `center_detent_snap` field documenting
  this under the `sweep` control's `indicator` section

**Build + pluginval verification (2026-08-26, post Phase 5.1):** Confirmed
- `./scripts/build-and-install.sh Transitionist` ran clean end-to-end (all 7
  phases) - VST3 (4.3M) and AU (4.2M) installed to
  `~/Library/Audio/Plug-Ins/`, both arm64 Mach-O, DAW caches cleared
- Found + fixed a real bug in `scripts/build-and-install.sh` along the way:
  Phase 5 didn't `mkdir -p` the destination before `cp -R`, so on a machine
  with no prior VST3 install the destination folder itself got overwritten
  by the bundle contents instead of nesting the bundle inside it. Fixed by
  adding `mkdir -p "$vst3_dir"` / `mkdir -p "$au_dir"` before each copy
  (commit `e70cbd5`) - this was a latent bug that could have bitten any
  plugin's first-ever install on a fresh machine, not Transitionist-specific
- `pluginval --strictness-level 5` run on both installed formats: SUCCESS
  for VST3 (all sample rates 44.1k/48k/96k x block sizes 64-1024) and
  SUCCESS for AU - no assertion failures, no crashes, automation/state/bus
  tests all pass
- This confirms Stage 2 DSP (all 3 phases) and Stage 3 GUI Phase 5.1 are
  solid on disk, not just source-complete - safe to proceed to Phase 5.2

**Stage 3 GUI - Phase 5.1 (Layout and Basic Controls):** Complete (2026-08-26)
- `Source/ui/public/index.html` copied from finalized production `v4-ui.html`
- `Source/ui/public/js/juce/index.js` and `check_native_interop.js` copied
  verbatim from GainKnob (identical JUCE-provided WebView bridge scripts
  across this codebase's plugins)
- `Source/PluginEditor.h/.cpp` replaced with real WebView editor adapted
  from `v4-PluginEditor.h/.cpp`: only change needed was `.apvts` ->
  `.parameters` to match this codebase's actual APVTS member name
- 3 relays (`throwRelay`/`spaceRelay`/`sweepRelay`, all `WebSliderRelay`) ->
  `webView` (`std::unique_ptr<juce::WebBrowserComponent>`) -> 3 attachments
  (`WebSliderParameterAttachment`, 3-arg constructor incl. `nullptr`
  undoManager), declared and constructed in that exact order
  (relays -> webView -> attachments, per Pattern #11)
- Explicit `getResource()` URL mapping (Pattern #8) serving `/`,
  `/index.html`, `/js/juce/index.js`, `/js/juce/check_native_interop.js`
  (`application/javascript` MIME type for both .js files); no other asset
  paths requested by `index.html` (self-contained inline CSS/JS, no fonts
  or images)
- `setSize(980, 420)` + `setResizable(false, false)` per v4-ui.yaml
- `sweep` bound with the same `WebSliderRelay`/`WebSliderParameterAttachment`
  pattern as `throw`/`space` - no bipolar-specific C++ needed (per
  parameter-spec.md; bipolar range already declared in Stage 1 APVTS,
  bipolar arc-fill/signed LCD already implemented in production HTML's
  `makeBipolarRenderer()`)
- `CMakeLists.txt`: uncommented the previously-staged WebView resource
  block (`juce_add_binary_data(Transitionist_UIResources ...)` for all 3
  UI files, linked privately, `JUCE_WEB_BROWSER=1`/`JUCE_USE_CURL=0`
  defined); `juce::juce_gui_extra` and `NEEDS_WEB_BROWSER TRUE` were
  already present from Stage 1 and left untouched
- Build verification NOT run by gui-agent per its role boundaries - handed
  off to plugin-workflow's build-automation skill

**Stage 2 DSP - Phase 4.3 (Advanced Features) - FINAL DSP PHASE:** Complete (2026-08-26)
- Reverb Modulation (Component 5): hand-rolled sine LFO (~0.15Hz phase
  accumulator) modulating a dedicated `juce::dsp::DelayLine` (Lagrange3rd,
  2-8ms range, 15ms max allocation), inserted after reverb, before filter;
  active unconditionally including during freeze; primary hand-rolled
  approach worked cleanly, Chorus fallback not needed
- Output Glue (Component 7): `juce::dsp::WaveShaper` (`tanh(1.15*x)`) +
  `juce::dsp::Limiter` (-1.0dB/50ms), inserted after Bipolar DJ Filter,
  before Stereo Width, wet-only
- Stereo Width (Component 8): manual M/S encode/decode, fixed 1.35x side
  gain, inserted after Output Glue, before Dry/Wet Mixer, wet-only,
  defensive numChannels>=2 guard
- All 9 architecture.md DSP components now implemented; Stage 2 DSP complete
- Chain order double-checked against architecture.md's Processing Chain
  diagram: Delay -> Reverb -> Reverb Modulation -> Bipolar Filter -> Output
  Glue -> Width -> Dry/Wet Mix (exact match)
- Real-time safety re-verified: all new state (DelayLine, WaveShaper,
  Limiter) prepared/sized in `prepareToPlay()` only; parameters still read
  once per block; `ScopedNoDenormals` still present; no new heap allocation
  in `processBlock()`

**Stage 2 DSP - Phase 4.2 (Parameter Modulation):** Complete (2026-08-26)
- Reverb freeze/hold: `freezeMode = smoothstep(throwNorm, 0.90, 1.00)`,
  explicit `reverbInputGain = 1.0 - freeze` mute applied before
  `reverb.process()` (does not mute the reverb's own sustaining output)
- Feedback saturation drive now throw-scaled: `1.0 + 0.6 * throwNorm`
  (1.0 at throw=0%, 1.6 at throw=100%), no output re-normalization
- Reused existing `smoothstep()` helper (no duplication); no new member
  variables needed (both additions are local per-block computations)

**Ideation:** Complete (revised 2026-08-26) - 3-macro redesign (throw/space/sweep),
UI mockup v4 finalized, parameter-spec.md locked.

**Stage 1 (Foundation + Shell):** Complete (2026-08-26)
- CMakeLists.txt created: `PRODUCT_NAME "Transitionist"`, `PLUGIN_MANUFACTURER_CODE Plgf`,
  `PLUGIN_CODE Trns`, `FORMATS VST3 AU Standalone`, `NEEDS_WEB_BROWSER TRUE`
  (WebView resource linkage left commented out for Stage 3 GUI to uncomment,
  since `Source/ui/public/index.html` does not exist yet)
- `Source/PluginProcessor.h/.cpp`: stereo-in/stereo-out `BusesProperties` in
  constructor (no MIDI bus, no `IS_SYNTH`), APVTS member named `parameters`
  (matches this codebase's established convention), 3 parameters implemented
  in exact locked order (`throw`, `space`, `sweep`), `sweep` declared bipolar
  (-100.0 to +100.0, default 0.0 at midpoint) with a `jassert` verifying
  `convertTo0to1(0.0f) == 0.5f` in the constructor, `getStateInformation`/
  `setStateInformation` delegate directly to APVTS (no custom ValueTree
  fields), `getLatencySamples()` left at JUCE's default 0 (never overridden,
  per architecture.md's Latency section), `processBlock()` is a `ScopedNoDenormals`
  + pass-through stub (buffer untouched - Stage 3 DSP owns all processing)
- `Source/PluginEditor.h/.cpp`: minimal non-WebView placeholder (600x400,
  displays parameter count) - compiles and opens; Stage 3 GUI replaces this
  entirely with the pre-scaffolded v4 WebView editor
  (`plugins/Transitionist/.ideas/mockups/v4-PluginEditor.h/.cpp`)
- Self-validation: all 3 parameter IDs (`throw`, `space`, `sweep`) confirmed
  present in `createParameterLayout()`, types match spec (all
  `AudioParameterFloat`), zero drift from parameter-spec.md
- Build verification NOT run by foundation-shell-agent per its role
  boundaries - handed off to plugin-workflow's build-automation skill

**Stage 0:** Research & Planning complete (2026-08-26)
- Plugin type: Audio Effect (Transition/Tail Effect)
- Signal chain researched and documented in full: Delay (hand-built feedback
  loop w/ tanh saturator + FirstOrderTPTFilter) -> Reverb (juce::dsp::Reverb +
  built-in freezeMode for "throw and hold") -> Reverb Modulation (custom
  LFO-modulated micro-delay) -> Bipolar DJ Filter (dual juce::dsp::LadderFilter
  instances + dead-zone/smoothstep crossfade, no direct JUCE precedent) ->
  Output Glue (WaveShaper soft-clip + Limiter) -> Stereo Width (M/S, wet-only,
  fixed constant) -> Dry/Wet Mixer (equal-power)
- Key research finding: juce::dsp::Reverb already ships a purpose-built
  freezeMode parameter - the custom/FDN reverb the creative brief flagged as a
  possible necessity is NOT required for v1 (retained only as a documented
  fallback)
- Professional plugins researched: Valhalla Supermassive/FutureVerb (freeze
  design philosophy), Soundtoys EchoBoy (tape delay feedback-loop saturation/
  darkening), Pioneer DJM-style one-knob filters (bipolar sweep concept)
- JUCE modules identified: juce::dsp::DelayLine (Lagrange3rd), FirstOrderTPTFilter,
  juce::dsp::Reverb, juce::dsp::LadderFilter, juce::dsp::WaveShaper,
  juce::dsp::Limiter, juce::AudioPlayHead::getPosition()/getBpm() (JUCE 8
  non-deprecated tempo-sync API)
- Highest-risk component identified: Bipolar DJ Filter (Component 6) - dual
  always-running LadderFilter instances + 3-way crossfade (bypass/LPF24/HPF24)
  is bespoke engineering with no direct JUCE precedent; must be
  prototyped/tested in isolation first (slow sweep -100%->+100%, check for
  any click/discontinuity at center)
- Complexity score: 5.0 (capped; raw total 11.6 = 0.6 param score + 9 DSP
  components + 2 feature points [feedback loop, modulation system])
- Strategy: Phased implementation - Stage 3 DSP in 3 phases (4.1 Core
  Processing, 4.2 Parameter Modulation/freeze, 4.3 Advanced
  Features/modulation+glue+width), Stage 3 GUI in 3 phases (5.1 Layout
  integration of already-generated v4 scaffolding, 5.2 Parameter Binding incl.
  bipolar sweep verification, 5.3 Advanced UI regression verification)
- Plan documented in full in plan.md, including per-phase test criteria

## Next Steps

1. Build-automation skill should build + install Transitionist and confirm:
   (a) all Phase 4.3 DSP test criteria from plan.md (reverb tail
   "aliveness"; Output Glue transparent at low throw and containing the
   worst-case combined throw=100%/space=100%/sweep-extreme state; Stereo
   Width audible on wet vs. narrow dry; mono-sum spot check; CPU usage
   ~25-35% single core at 256-512 samples); (b) Phase 5.1 GUI test criteria
   from plan.md: WebView window opens at 980x420 non-resizable, all 3 knob
   panels visible/styled per mockup (brushed-metal discs, LCD readouts,
   panel cards, gray textured outer frame), `NEEDS_WEB_BROWSER TRUE` set
   (VST3 loads in DAW not just AU), `check_native_interop.js` present and
   served with no console errors/404s. Also run pluginval.
2. Stage 3 GUI Phase 5.1 (Layout and Basic Controls) is complete. Next:
   Phase 5.2 (Parameter Binding and Interaction) - full two-way parameter
   sync verification (drag-to-rotate, host automation, preset recall), with
   explicit attention to `sweep`'s bipolar behavior: exact-center (0.5
   normalised) displays "0%" with zero arc fill and the fixed 12-o'clock
   tick visible, correct signed `-NN%`/`+NN%` display on either side of
   center, double-click resets to 0% (center) not the range minimum. The
   interaction JS (drag/wheel/keyboard/dblclick, bipolar renderer) is
   already implemented in `Source/ui/public/index.html` (copied verbatim
   from the browser-tested `v4-ui.html`) - Phase 5.2 is primarily
   verification inside the real WebView-hosted plugin, not new JS
   authoring, per plan.md's phase split.
3. Phase 5.3 (Advanced UI Elements) after that: regression verification of
   the bipolar arc-fill/center-detent tick/LCD rendering fidelity inside the
   actual plugin WebView vs. the standalone `v4-ui-test.html` browser
   mockup - visual polish confirmation only, not new implementation.

## Context to Preserve

**Key Decisions (Stage 0, in addition to prior ideation decisions in mockup
history below):**
- Reverb freeze/hold implemented via `juce::dsp::Reverb::Parameters::freezeMode`
  (built-in), ramped via smoothstep across throw 90%-100%, NOT a custom FDN
  reverb (custom FDN retained only as documented fallback)
- Bipolar DJ Filter implemented via TWO always-running `juce::dsp::LadderFilter`
  instances (LPF24 pinned, HPF24 pinned) with a dead-zone (|sweep|<1%) +
  smoothstep crossfade (1%-5%) between a true dry-bypass tap and whichever
  filter matches sweep's sign - avoids any runtime `setMode()` switching and
  its associated click risk
- Dry/wet mix (throw) uses an equal-power (sin/cos) crossfade, not linear,
  because throw simultaneously increases wet-path density/loudness through
  several other mechanisms
- Output Glue and Stereo Width are both fixed internal constants (not
  parameters) - soft-clip drive 1.15, limiter -1.0dB/50ms release, M/S side
  gain 1.35x - preserving the locked 3-parameter contract
- Reverb Modulation (LFO-modulated micro-delay, ~0.15Hz/+-3ms) is the single
  most explicitly "okay to drop" component if it proves hard to tune -
  documented fallback: juce::dsp::Chorus, then no modulation at all
- Tempo sync for `space` uses a 10-step division table (1/32 through whole
  note) with `SmoothedValue`-ramped delay-time changes (~50ms) to avoid
  clicks on division changes; falls back to 120 BPM if host reports no tempo
- 0 samples reported latency - no component in this architecture requires PDC

**Prior decisions carried from Ideation (unchanged):**
- Signal chain (fixed order): Delay -> Reverb -> Bipolar DJ Filter -> Output
  Glue (soft-clip/limiter) -> Width (M/S, wet-only)
- THROW: triple-role macro (dry/wet, delay feedback, reverb decay/size), gates
  reverb freeze/hold near top of range, lightly scales feedback-loop saturation
- SPACE: links tempo-synced delay division + reverb size into one macro,
  independent of THROW
- SWEEP: bipolar ladder filter - center=open, CCW=24dB lowpass closing,
  CW=24dB highpass opening, resonance rises toward both extremes
- v3/v4 UI direction: light skeuomorphic hardware aesthetic - LOCKED; only
  knob count/labels/bindings should change in future iterations
- Parameter IDs LOCKED per `parameter-spec.md`: `throw`, `space`, `sweep`

**Files Created/Modified (Stage 3 GUI - Phase 5.1):**
- plugins/Transitionist/Source/ui/public/index.html (copied from v4-ui.html)
- plugins/Transitionist/Source/ui/public/js/juce/index.js (copied from GainKnob)
- plugins/Transitionist/Source/ui/public/js/juce/check_native_interop.js (copied from GainKnob)
- plugins/Transitionist/Source/PluginEditor.h (replaced - real WebView editor)
- plugins/Transitionist/Source/PluginEditor.cpp (replaced - real WebView editor)
- plugins/Transitionist/CMakeLists.txt (WebView resource block uncommented)

**Files Created (Stage 1):**
- plugins/Transitionist/CMakeLists.txt
- plugins/Transitionist/Source/PluginProcessor.h
- plugins/Transitionist/Source/PluginProcessor.cpp
- plugins/Transitionist/Source/PluginEditor.h (Stage 1 placeholder, superseded by Stage 3 above)
- plugins/Transitionist/Source/PluginEditor.cpp (Stage 1 placeholder, superseded by Stage 3 above)

**Files Created (Stage 0):**
- plugins/Transitionist/.ideas/architecture.md (DSP architecture, 11 required
  sections, all 9 core components documented with risks/fallbacks)
- plugins/Transitionist/.ideas/plan.md (complexity score 5.0, phased strategy,
  6 total implementation phases with test criteria)

**Files Created (prior stages, unchanged):**
- plugins/Transitionist/.ideas/creative-brief.md
- plugins/Transitionist/.ideas/parameter-spec.md
- plugins/Transitionist/.ideas/mockups/v1-ui.yaml, v1-ui-test.html
- plugins/Transitionist/.ideas/mockups/v2-ui.yaml, v2-ui-test.html
- plugins/Transitionist/.ideas/mockups/v3-ui.yaml, v3-ui-test.html
- plugins/Transitionist/.ideas/mockups/v4-ui.yaml (finalized: true)
- plugins/Transitionist/.ideas/mockups/v4-ui-test.html
- plugins/Transitionist/.ideas/mockups/v4-ui.html (production HTML)
- plugins/Transitionist/.ideas/mockups/v4-PluginEditor.h/.cpp (C++ templates)
- plugins/Transitionist/.ideas/mockups/v4-CMakeLists.txt (CMake snippet)
- plugins/Transitionist/.ideas/mockups/v4-integration-checklist.md
