---
plugin: KICKR
stage: 2
phase: 2.2
status: complete
last_updated: 2026-08-28
complexity_score: 5.0
phased_implementation: true
orchestration_mode: true
next_action: invoke_dsp_agent
next_stage: 2
next_phase: 2.3
ready_for_implementation: true
spec_version: v2
param_count: 59
contract_checksums:
  creative_brief: sha256:a9e3cc7bd083132cde04df49387a4e0eafae530f387c4c0a671c20140bac5f7c
  parameter_spec: sha256:ee48990b2006afb3ab93f51d9dfdf358b45280e03cc5fb5bb28cce9326bae618
  architecture: sha256:7c07d42e79265c4aa317b3e8c5ea0957bbf7c3bca3346a3c3a776a8f229e4beb
  plan: sha256:0538295685a81ce25fc618abb5045eaca0913b2d496b18b0c86f0b0357dffa2f
  research_notes: sha256:8145189fc79baf8286ca0f139107eb4928a462b88227a4dbdaed5070c35ca82d
---

# Resume Point

## Current State: Stage 2 — DSP Phase 2.2 complete

Pitch envelope now live. `PitchEnvelope` (header-only, `Source/DSP/PitchEnvelope.h`)
implements the ratio/log-domain snap+settle contour:
`tau = clamp(tSinceTrigger / pitchTime, 0, 1)`, `k = 0.6 + pitchCurve*8.4`,
`e(tau) = (exp(-k*tau) - exp(-k)) / (1 - exp(-k))` (1→0, exactly 0 at tau=1),
`f = fundamentalEff * pitchStart^e(tau)`. Never lerps Hz; frequency only —
BodyOscillator integrates phase and is never reset mid-fall (click-free).
`k`, `exp(-k)`, `1/(1-exp(-k))`, `log2(pitchStart)` computed once per block in
`setParams()`; per sample = one `std::exp` + one `std::exp2`. Output sanitised +
clamped to [10, 20000] Hz. Runs inside the OS region (`tSinceTrigger` advances by
`1/fsOversampled`).

`KickEngine` snapshots `pitchStart` / `pitchTime` / `pitchCurve` per block, computes
the velocity-scaled effective `pitchStart`
(`1 + (pitchStart-1)*lerp(1, 0.5+0.5*v01, velSensitivity*0.35)` — architecture row 2),
and calls `KickVoice::setPitchParams(...)` both per block (automation stays live) and
right after each `noteOn` (fresh contour uses that note's velocity from sample 0).

### Prior state: Phase 2.1

OS-region shell (AD-10) substrate: `processSamplesUp`(zero block) → whole-voice
render @ `fsOversampled` → `processSamplesDown` → base-rate ~5 Hz DC blocker +
NaN/Inf guard. `oversampling` pinned to 1x. MIDI note-on (sample-accurate sub-block
split) → monophonic sine `KickVoice` (BodyOscillator + AmplitudeEnvelope, 2 ms
raised-cosine attack + exp decay), pitch resolved for both tune modes, velocity → level.

**Phase 2.2 files:** `Source/DSP/PitchEnvelope.h` (passthrough stub → real contour),
`KickVoice.{h,cpp}` (`setPitchParams` forwarder), `KickEngine.{h,cpp}`
(3 cached ptrs + snapshot fields + `effectivePitchStartRatio` + `lastVel01`).
No new translation units — `PitchEnvelope` stays header-only, no CMake change.

**Not yet built/validated by the orchestrator** — build + 0-warnings check +
Phase 2.2 checkpoint (sweep shape, reaches fundamental at pitchTime, click-free
through the fall, curve=0 vs curve=1) run after this agent returns.

### Prior state (Stage 1 + v2 amendment)
Buildable synth shell with the full **59-parameter** APVTS, output-only stereo bus,
UndoManager-backed state round-trip (`stateVersion = 2`, `currentSampleName`), native
editor stub. Rebuilt clean VST3+AU+Standalone · pluginval strictness 10 SUCCESS ·
auval "59 Global Scope Parameters" SUCCEEDED.

## Completed So Far

**Ideation:** ✓ Complete — creative brief, 🔒 locked parameter spec **v2** (59 APVTS: 51 Float / 3 Choice / 5 Bool), deep-research notes.

**Stage 0:** ✓ Complete (+ AD-10 post-review revision) — architecture.md + plan.md; complexity 5.0 (capped) → phased; all 10 Open Questions resolved. PLUGIN codes `PLUGIN_MANUFACTURER_CODE Plgf` / `PLUGIN_CODE Kckr` / `COMPANY_NAME "PluginFreedom"`.

**Renamed 2026-08-28:** "Kick Designer 2" → **KICKR** (folder `plugins/KICKR/`, CMake target `KICKR`, `PRODUCT_NAME "KICKR"`, C++ classes `KICKRAudioProcessor`/`Editor`, namespace `kickr::`, `PLUGIN_CODE` `Kd02`→`Kckr`). All contracts, source, mockup, PLUGINS.md updated. Old `Kick Designer 2.component` removed from `~/Library/Audio/Plug-Ins/Components/`.

**`fundamental` shows as a note (2026-08-28):** displayed/dialled as a musical note (55 Hz = A1), Hz in the tooltip. Param stays a continuous Hz float (`AudioParameterFloat` 25–150); `hzToNoteName`/`noteNameToHz` in `ParameterLayout.h` via `withStringFromValueFunction`/`withValueFromStringFunction`. Semitone-snap on plain drag is a Stage-3 Knob behaviour.

**No separate Pitch panel (2026-08-28):** the PITCH group's 4 params (`fundamental`, `pitchStart`, `pitchTime`, `pitchCurve`) are **rendered inside the BODY panel** — `fundamental` is Body's hero **NOTE** knob; `pitchStart`/`pitchTime`/`pitchCurve` join `bodyLevel`/`bodyDecay`/`bodyHarmonics` as its small-knob cluster. Parameters unchanged (still 59 APVTS). Engine strip went 8 → 7 cells. UI-only; parameter-spec/creative-brief/architecture UI notes + mockup updated.

**v2 SAMPLE engine (2026-08-28, user request):** blendable 6th sample-playback layer (`SamplePlayer`) fed from a drag-drop managed bank (`SampleLibrary`, `…/KICKR/Samples/`). `synthEnable` on / `sampleEnable` off by default → v1 sound unchanged. 14 new params, `currentSampleName` state property, `stateVersion` → 2. AD-11 (layer + bank) / AD-12 (no factory samples). All 4 contracts + UI mockup updated. New Stage-2 phase **2.7b** (SamplePlayer + SampleLibrary).

**Stage 1:** ✓ Complete (2026-08-28) — built at v1 (45 params), then **v2-amended** (→ 59 params, `stateVersion` 2, `currentSampleName`). Rebuilt + revalidated.
- `CMakeLists.txt` — `juce_add_plugin(... IS_SYNTH TRUE NEEDS_MIDI_INPUT TRUE IS_MIDI_EFFECT FALSE FORMATS VST3 AU Standalone PRODUCT_NAME "KICKR")`; links `juce_dsp` + `juce_audio_utils` + `juce_audio_formats` + standard modules; `juce_generate_juce_header` AFTER `target_link_libraries` (Pattern #1); no WebView; factory-preset `juce_add_binary_data` left commented for Phase 3.3.
- `Source/PluginProcessor.{h,cpp}` — output-only stereo `BusesProperties` in the constructor (Pattern #4 / #22); `isBusesLayoutSupported` accepts stereo/mono out, rejects input; `juce::UndoManager` attached to the APVTS; `getStateInformation`/`setStateInformation` round-trip with `stateVersion = 2` + `currentPresetName`/`currentPresetPath`/**`currentSampleName`** properties; v1 states load fine; `setLatencySamples(0)`; `processBlock` = `ScopedNoDenormals` + `buffer.clear()`.
- `Source/Parameters/ParameterIDs.h` — **59** `constexpr` ID strings (single source of truth; SAMPLE group added).
- `Source/Parameters/ParameterLayout.h` — table-driven `kickr::createParameterLayout()`: **51** `AudioParameterFloat` from a `std::array<FloatParamSpec, 51>` (41 v1 + 10 SAMPLE, non-deprecated `AudioParameterFloatAttributes` signature), 3 `AudioParameterChoice` (`noiseType` idx0, `oversampling` idx1, `tuneMode` idx0), **5** `AudioParameterBool` (`limiter` true, `synthEnable` true, `sampleEnable` false, `sampleReverse` false, `sampleMidiTrack` true).
- `Source/PluginEditor.{h,cpp}` — native `AudioProcessorEditor` stub, resizable (`setResizeLimits` + fixed aspect), placeholder text.
- `Source/Utilities/DSPUtils.{h,cpp}` — `flushDenormal`, `sanitize`, `dbToGain`/`gainToDb`, `expDecayCoef`, `pitchRatio`, `equalPowerGains`, `DCBlocker`, `sanitizeBuffer`.
- `Source/Tests/OfflineRender.{h,cpp}` — functional headless host loop (prepare → note-on at sample 0 → render N s → release) + 24-bit WAV writer. Renders silence until Stage 2.
- `Source/DSP/*.h` — stub headers for KickEngine, KickVoice, BodyOscillator, PitchEnvelope, AmplitudeEnvelope, SubOscillator, ClickGenerator, NoiseGenerator, TailGenerator, TransientShaper, Saturator, Waveshaper, OutputStage, OversamplingProcessor (4-instance `dsp::Oversampling` member layout + `prepare()`/`reset()` signatures per AD-10).
- `Source/Presets/PresetManager.h` + `Source/UI/LookAndFeel.h` — stubs for Stage 3.

**Stage 2 — DSP Phase 2.1:** ✓ Complete (2026-08-28) — OS-region shell (AD-10) + MIDI-triggered sine body + amp env.
- `Source/DSP/OversamplingProcessor.{h,cpp}` — 4 pre-built `juce::dsp::Oversampling<float>` (1x/2x/4x/8x, `filterHalfBandPolyphaseIIR`, `isMaxQuality`, `useIntegerLatency`, `numChannels=2`); `initProcessing` on all 4; **`activeOrder` pinned to 0 (1x)** — real 1x is JUCE's dummy-stage object (0 latency, identity). `processSamplesUp`/`processSamplesDown` delegate to the active instance; `getLatencySamples()` = `round(getLatencyInSamples())` = 0. `setFactorChoice()` is a Phase-2.10 no-op hook.
- `Source/DSP/BodyOscillator.{h,cpp}` — phase-accumulator + `std::sin`; `setFrequency` per-sample-safe; `noteOn()` → phase 0; `renderSample()` wraps + sanitizes.
- `Source/DSP/AmplitudeEnvelope.{h,cpp}` — AD; `kBodyAttackMs = 2.0` raised-cosine attack; `dsputils::expDecayCoef` decay; `isActive()` false below −90 dB and past `attack + 5 ms`; `flushDenormal` on the running value.
- `Source/DSP/KickVoice.{h,cpp}` — BodyOsc + AmpEnv + `PitchEnvelope` (passthrough); `noteOn(freqHz, note, velLevelGain, bodyDecayMs, offset)`; `renderAdd(AudioBlock&, start, num)` adds `osc · env · bodyLevel · velLevel` into every channel; `bodyLevel` via a 20 ms `SmoothedValue` snapped to target on each trigger.
- `Source/DSP/KickEngine.{h,cpp}` — owns `OversamplingProcessor` + one `KickVoice` + 2 `dsputils::DCBlocker`; caches raw APVTS pointers in `prepare`; per-block snapshot of the 8 live params; sample-accurate MIDI sub-block split; pitch resolver (MIDI-Pitch = `noteHz · fundamental/55` per AD-6, Fixed-Frequency = `fundamental`, then `pitchRatio(tune + fineTune/100)`, clamp 10–5000 Hz); velocity → level `lerp(1, v/127, velSensitivity)`; per sub-block `processSamplesUp(zero)` → `voice.renderAdd` → `processSamplesDown` → base-rate ~5 Hz DC blocker + `sanitize`. Analyzer tap = commented Phase-3.2 hook.
- `Source/DSP/PitchEnvelope.h` — updated to a header-only passthrough stub (`prepare(double)`, `noteOn()`, `nextFrequency(base) → base`); Phase 2.2 fills the contour math.
- `Source/PluginProcessor.{h,cpp}` — `kickr::KickEngine engine { apvts }` member; `engine.prepare` in `prepareToPlay` + `setLatencySamples(engine.getLatencySamples())`; `engine.reset()` in `releaseResources`; `engine.processBlock(buffer, midi)` replaces the `buffer.clear()` stub.
- `CMakeLists.txt` — 5 new `.cpp` added to `target_sources(KICKR ...)`.

**Stage 2 — DSP Phase 2.2:** ✓ Complete (2026-08-28) — Pitch envelope (ratio/log-domain snap+settle).
- `Source/DSP/PitchEnvelope.h` — real contour (header-only). `setParams(startRatioEff, timeMs, curve)` per-block coefficient refresh (`k = 0.6 + pitchCurve·8.4`, `exp(-k)`, `1/(1-exp(-k))`, `log2(pitchStart)`); `nextFrequency(fundamentalEff)` per sample = `fundamentalEff · pitchStart^e(τ)` with `e(τ) = (exp(-k·τ) − exp(-k)) / (1 − exp(-k))`, `τ = tSinceTrigger / pitchTime` (accum by `1/fsOversampled`), exact `fundamentalEff` for `τ ≥ 1`; sanitised + clamped [10, 20000] Hz. Frequency only — phase stays continuous in BodyOscillator.
- `Source/DSP/KickVoice.{h,cpp}` — `setPitchParams(startRatioEff, timeMs, curve)` forwards to `pitchEnv.setParams`; `renderAdd` already fed `pitchEnv.nextFrequency(baseFrequencyHz)` → `body.setFrequency`.
- `Source/DSP/KickEngine.{h,cpp}` — cached `pPitchStart`/`pPitchTime`/`pPitchCurve`; snapshot `pitchStartRatio`/`pitchTimeMs`/`pitchCurve`; `effectivePitchStartRatio(v01)` = `1 + (pitchStart−1)·lerp(1, 0.5+0.5·v01, velSensitivity·0.35)` (architecture Parameter Mapping row 2 — velocity subtle); `lastVel01` stored on trigger; `voice.setPitchParams(...)` per block + immediately after `noteOn`.
- Params live: `pitchStart`, `pitchTime`, `pitchCurve` (+ `velSensitivity` now also routes subtly to `pitchStart`).
- Real-time safe: no alloc/lock/log/IO; one `exp`/`log2` per block, one `exp` + one `exp2` per sample (monophonic); `tSinceTrigger` is monotonic time (no denormal risk); output sanitised.
- No new translation units / no CMake change — `PitchEnvelope` is header-only.

## Next Steps

1. **Orchestrator:** build (0 warnings, VST3+AU+Standalone) + run the Phase 2.2 checkpoint:
   - Offline WAV shows exponential frequency fall from `fundamental×pitchStart` → `fundamental`, reaching fundamental at `pitchTime`.
   - `pitchCurve` 0 = audible sweep, 1 = snap+settle (spectrogram / instantaneous-freq check).
   - Zero phase discontinuity / clicks at trigger and through the fall.
   - Default patch (55 Hz, ×4, ~50 ms, curve 0.7) reads as punch, not laser.
2. **Stage 2 — Phase 2.3 (Amp-env refinement + transient shaper + click-free retrigger):** `KickVoice voices[2]` + `active` toggle, 3 ms equal-power crossfade, `TransientShaper` (dual envelope follower, ±6 dB). Params: `transientAttack`, `transientSustain`.
3. Continue Stage 2 phases 2.4 → 2.11 + 2.7b, then Stage 3 GUI (4 phases) + Validation.
3. Repo Stage 17 — profile → lock `oversampling` default (2× vs 4×).
4. Repo Stage 18 — release; then set `MinimalKick` → 🗑️ Archived.

**UI:** concept mockup at `.ideas/mockups/v1-ui-concept.html` (Artifact `8c27d484-147e-401c-8ddb-f2ddf8673f0d`) — soft slate-grey chassis (ref-sampled `#31353F`), kick oscilloscope, PUNCH/BODY/CRUSH/TAIL macro modules, 8-cell engine strip. **Needs a v3 pass to add the SAMPLE module** (enable toggles + drag-drop bank + tweak knobs).

## Context to Preserve

**Key decisions (Stage 0):**
- **Oversampling scope (AD-10):** OS region wraps the whole voice + master chain through the `tanh` safety limiter. Only DC blocker + analyzer taps at base rate. `processSamplesUp` fed a zero block. All in-region coefficients use `fsOversampled`, recomputed on factor change. `8×` = "high CPU"; Stage-17 base-rate escape hatch for pure-sine layers.
- Oversampling default `2x` provisional (index 1); 4 `dsp::Oversampling` (`numChannels=2`) pre-built per factor, atomic swap + 64-sample fade, `useIntegerLatency = true`.
- Safety limiter = soft-clip `tanh` ceiling −0.5 dBFS, in-region, 0 latency; no lookahead limiter in v1.
- Distortion morph order (parameter-spec authoritative): `tanh→cubic→asymmetric→soft-clip→hard-clip→foldback→bitcrush`; equal-gain crossfade; adaptive RMS makeup (static LUT fallback).
- Tone fixed pre-distortion. Mono crossover fixed 130 Hz. Pitch env single `pitchCurve`-driven normalised-exp (`k = 0.6 + pitchCurve·8.4`); ratio/log domain; phase-continuous.
- `fundamental` in MIDI Pitch mode = global semitone offset `12·log2(fundamental/55)`.
- `mix` = equal-power processed↔silence (no dry path for instrument).
- `bodyAttack` fixed 2 ms internal (NOT a parameter).
- `output` 0 dB in −24..+12 → normalised ≈ 0.6667 (future Knob double-click-reset target).
- Native JUCE UI (custom LookAndFeel + Knob/WaveformDisplay/SpectrumDisplay/EnvelopeDisplay/PresetBrowser/**SampleBrowser**); analyzer via AbstractFifo + double buffer + 30 fps Timer FFT (message thread only).
- Presets: 17 factory via BinaryData (all synth-only) + user disk under `…/KICKR/`.

**v2 SAMPLE key decisions (AD-11 / AD-12):**
- 6th layer `SamplePlayer` sums with synth **before** TransientShaper → identical downstream chain (drive/tone/stereo/limiter/macros/OS apply). `sampleHP`/`LP`/`Crush` are sample-only, pre-sum, in-region.
- Bank = managed folder `…/KICKR/Samples/`; drag-drop copies files in; presets reference by bare name. **No factory samples.**
- Buffer: in-RAM `SampleBuffer` via `std::atomic<const SampleBuffer*>`; voice grabs the pointer at `noteOn`, holds it for its life; previous buffer retired on message thread once unreferenced. 5 s length cap. Formats WAV/AIFF/FLAC/CAF.
- `SamplePlayer` resamples straight to `fsOversampled` (in-region, no base-rate escape hatch). `sampleMidiTrack` root = C1 (24) in v1.
- Open for Stage 2: interpolation order (Lagrange vs WindowedSinc), per-sample root, `sampleDecay` max semantics.

**Contract files (all revised 2026-08-28 for v2):**
- `.ideas/creative-brief.md` — + Sample Engine section, non-goals reversed
- `.ideas/parameter-spec.md` — 🔒 locked **v2** (+ Stage 0 Addendum + AD-10 note + v2 Addendum)
- `.ideas/research-notes.md` — advisory (unchanged)
- `.ideas/architecture.md` — ✓ (AD-10 + AD-11/AD-12, SamplePlayer/SampleLibrary components)
- `.ideas/plan.md` — ✓ (Phase 2.7b, Stage 1 v2 Amendment, 59 params)
