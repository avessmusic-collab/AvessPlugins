---
plugin: KickDesigner2
stage: 1
phase: null
status: complete
last_updated: 2026-08-28
complexity_score: 5.0
phased_implementation: true
orchestration_mode: true
next_action: invoke_dsp_agent
next_stage: 2
next_phase: 2.1
ready_for_implementation: true
contract_checksums:
  creative_brief: sha256:d881c3024084ff342e094daec3f68f77c657da70268f53795e7cd500ef9e7255
  parameter_spec: sha256:57f33ca9b571a14ab2c78eef1a1328e153d769e48028e37e98ce4cb3e15b1495
  architecture: sha256:26a20111f08210bcc2a38268685b0d2a84591aa6d83c3383411eafa7397d10fd
  plan: sha256:959e35584c87b4190a5c76847b2ad63148ba090b3aa84a71a1b0da838e6863c2
  research_notes: sha256:89aa855d0155ee496f7e242943aa0bb1672a0433fc4785d1626ffc3f849ac58f
---

# Resume Point

## Current State: Stage 1 — Foundation + Shell Complete

Buildable synth shell with the full 45-parameter APVTS, output-only stereo bus,
UndoManager-backed state round-trip, and a native editor stub. Empty (silent)
engine — `processBlock` clears the buffer; MIDI is consumed but not synthesised.
Build verification is run by the orchestrator (build-automation) after this stage.

## Completed So Far

**Ideation:** ✓ Complete — creative brief, 🔒 locked parameter spec (45 APVTS params: 41 Float / 3 Choice / 1 Bool), deep-research notes.

**Stage 0:** ✓ Complete (+ AD-10 post-review revision) — architecture.md + plan.md; complexity 5.0 (capped) → phased; all 10 Open Questions resolved; PLUGIN codes `Plgf` / `Kd02` / `PluginFreedom`.

**Stage 1:** ✓ Complete (2026-08-28)
- `CMakeLists.txt` — `juce_add_plugin(... IS_SYNTH TRUE NEEDS_MIDI_INPUT TRUE IS_MIDI_EFFECT FALSE FORMATS VST3 AU Standalone PRODUCT_NAME "Kick Designer 2")`; links `juce_dsp` + `juce_audio_utils` + `juce_audio_formats` + standard modules; `juce_generate_juce_header` AFTER `target_link_libraries` (Pattern #1); no WebView; factory-preset `juce_add_binary_data` left commented for Phase 3.3.
- `Source/PluginProcessor.{h,cpp}` — output-only stereo `BusesProperties` in the constructor (Pattern #4 / #22); `isBusesLayoutSupported` accepts stereo/mono out, rejects input; `juce::UndoManager` attached to the APVTS; `getStateInformation`/`setStateInformation` round-trip with top-level `stateVersion = 1` + `currentPresetName`/`currentPresetPath` properties; `setLatencySamples(0)`; `processBlock` = `ScopedNoDenormals` + `buffer.clear()`.
- `Source/Parameters/ParameterIDs.h` — 45 `constexpr` ID strings (single source of truth).
- `Source/Parameters/ParameterLayout.h` — table-driven `kd2::createParameterLayout()`: 41 `AudioParameterFloat` from a `std::array<FloatParamSpec, 41>` (exact ranges/defaults/skews from parameter-spec.md; skew < 1 on `fundamental`/`subFreq`/`clickTone`/`clickPitch`), 3 `AudioParameterChoice` (`noiseType` White/Pink/Filtered idx0, `oversampling` 1x/2x/4x/8x idx1, `tuneMode` MIDI Pitch/Fixed Frequency idx0), 1 `AudioParameterBool` (`limiter` = true).
- `Source/PluginEditor.{h,cpp}` — native `AudioProcessorEditor` stub, resizable (`setResizeLimits` + fixed aspect), placeholder text.
- `Source/Utilities/DSPUtils.{h,cpp}` — `flushDenormal`, `sanitize`, `dbToGain`/`gainToDb`, `expDecayCoef`, `pitchRatio`, `equalPowerGains`, `DCBlocker`, `sanitizeBuffer`.
- `Source/Tests/OfflineRender.{h,cpp}` — functional headless host loop (prepare → note-on at sample 0 → render N s → release) + 24-bit WAV writer. Renders silence until Stage 2.
- `Source/DSP/*.h` — stub headers for KickEngine, KickVoice, BodyOscillator, PitchEnvelope, AmplitudeEnvelope, SubOscillator, ClickGenerator, NoiseGenerator, TailGenerator, TransientShaper, Saturator, Waveshaper, OutputStage, OversamplingProcessor (4-instance `dsp::Oversampling` member layout + `prepare()`/`reset()` signatures per AD-10).
- `Source/Presets/PresetManager.h` + `Source/UI/LookAndFeel.h` — stubs for Stage 3.

## Next Steps

1. **Stage 2 — DSP (11 phases 2.1 → 2.11):** start at **Phase 2.1** — establish the oversampling region as the substrate (`processSamplesUp`(zero block) → whole-voice render at `fsOversampled` → `processSamplesDown` → base-rate DC blocker + analyzer tap), `oversampling` pinned to `1x`; then MIDI-triggered sine body (BodyOscillator + AmplitudeEnvelope, 2 ms attack, exp decay) + pitch resolver (`tuneMode`/`tune`/`fineTune`/`fundamental`). Compile + offline-WAV checkpoint + zero warnings after each phase.
2. Stage 3 — GUI (4 phases 3.1 → 3.4) + Validation (17 presets, pluginval, offline-render tests, changelog).
3. Repo Stage 17 — profile → lock `oversampling` default (2× vs 4×).
4. Repo Stage 18 — release; then set `MinimalKick` → 🗑️ Archived.

## Context to Preserve

**Key decisions (Stage 0):**
- **Oversampling scope (AD-10):** OS region wraps the whole voice + master chain through the `tanh` safety limiter. Only DC blocker + analyzer taps at base rate. `processSamplesUp` fed a zero block. All in-region coefficients use `fsOversampled`, recomputed on factor change. `8×` = "high CPU"; Stage-17 base-rate escape hatch for pure-sine layers.
- Oversampling default `2x` provisional (index 1); 4 `dsp::Oversampling` (`numChannels=2`) pre-built per factor, atomic swap + 64-sample fade, `useIntegerLatency = true`.
- Safety limiter = soft-clip `tanh` ceiling −0.5 dBFS, in-region, 0 latency; no lookahead limiter in v1.
- Distortion morph order (parameter-spec authoritative): `tanh→cubic→asymmetric→soft-clip→hard-clip→foldback→bitcrush`; equal-gain crossfade; adaptive RMS makeup (static LUT fallback).
- Tone fixed pre-distortion. Mono crossover fixed 130 Hz. Pitch env single `pitchCurve`-driven normalised-exp (`k = 0.6 + pitchCurve·8.4`); ratio/log domain; phase-continuous.
- `fundamental` in MIDI Pitch mode = global semitone offset `12·log2(fundamental/55)`.
- `mix` = equal-power processed↔silence (no dry path for instrument).
- `bodyAttack` fixed 2 ms internal (NOT a parameter — do not add a 42nd float).
- `output` 0 dB in −24..+12 → normalised ≈ 0.6667 (future Knob double-click-reset target).
- Native JUCE UI (custom LookAndFeel + Knob/WaveformDisplay/SpectrumDisplay/EnvelopeDisplay/PresetBrowser); analyzer via AbstractFifo + double buffer + 30 fps Timer FFT (message thread only).
- Presets: 17 factory via BinaryData + user disk under `~/Library/Audio/Presets/PluginFreedom/KickDesigner2/`.

**Contract files:**
- `.ideas/creative-brief.md` — complete
- `.ideas/parameter-spec.md` — 🔒 locked v1 + Stage 0 Addendum + AD-10 note
- `.ideas/research-notes.md` — advisory
- `.ideas/architecture.md` — ✓ Stage 0 (revised 2026-08-28: AD-10)
- `.ideas/plan.md` — ✓ Stage 0 (revised 2026-08-28: AD-10)
